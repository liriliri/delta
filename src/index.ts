import * as mathTrig  from './math-trig';
import * as convertor from './convertor';

export let {
    ABS,
    ACOS,
    ACOSH,
    ACOT,
    ACOTH,
    INT
} = mathTrig;

export let {
    toDouble
} = convertor;